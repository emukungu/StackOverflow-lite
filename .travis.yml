language: python

python:
  - "3.6"

# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install coveralls
  - pip install pytest
# command to run tests
script:
  - coverage run -m pytest --cover-package=test
  - coverage report

after_success:
  - coveralls
